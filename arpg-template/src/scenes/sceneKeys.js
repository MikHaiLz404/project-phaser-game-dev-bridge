// Canonical Phaser scene keys. Import these instead of duplicating string
// literals across registration, scene lookup, and launch call sites.
export const GAME_SCENE_KEY = 'Game';
export const UI_SCENE_KEY = 'UI';
